import React, { FC } from "react"

interface Props {
  show: boolean
}

const FilterBenefits: FC<Props> = ({ show }) => {
  return (
    <>
      <div>Filter Benefits</div>
    </>
  )
}

export default FilterBenefits
